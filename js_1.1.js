const string = "Hello world";
const number = "2" + 100;
const person = { firstName: "omar", lastName: "jo", age: 10 };
const boolean = true;
const nullified = null;
const undef = undefined;
const symbol = "@?";
console.log(
  `${string},${number * 2},${person},${boolean},${nullified},${undef},${symbol}`
);
